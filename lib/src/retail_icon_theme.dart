/// The visual theme for an [IconPark] icon.
enum IconParkTheme {
  /// A stroke-only icon with no fill.
  outline,

  /// A filled icon with no visible stroke.
  filled,

  /// An icon rendered with two distinct colors.
  twoTone,

  /// An icon rendered with up to four distinct colors.
  multiColor,
}

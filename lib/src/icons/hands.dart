// GENERATED FILE — DO NOT EDIT BY HAND.
// Run `cd tool && npm run generate` to regenerate.
//
// Category: hands

import '../retail_icon_data.dart';

// ignore_for_file: lines_longer_than_80_chars, constant_identifier_names

/// IconPark icons — category: hands
abstract final class RetailIconsHands {
  /// bad
  static const RetailIconData bad = RetailIconData.bad;

  /// bad-one
  static const RetailIconData badOne = RetailIconData.badOne;

  /// bad-two
  static const RetailIconData badTwo = RetailIconData.badTwo;

  /// bless
  static const RetailIconData bless = RetailIconData.bless;

  /// bye
  static const RetailIconData bye = RetailIconData.bye;

  /// clap
  static const RetailIconData clap = RetailIconData.clap;

  /// click-tap
  static const RetailIconData clickTap = RetailIconData.clickTap;

  /// click-tap-two
  static const RetailIconData clickTapTwo = RetailIconData.clickTapTwo;

  /// come
  static const RetailIconData come = RetailIconData.come;

  /// concept-sharing
  static const RetailIconData conceptSharing = RetailIconData.conceptSharing;

  /// cool
  static const RetailIconData cool = RetailIconData.cool;

  /// delivery
  static const RetailIconData delivery = RetailIconData.delivery;

  /// easy
  static const RetailIconData easy = RetailIconData.easy;

  /// empty
  static const RetailIconData empty = RetailIconData.empty;

  /// fingerprint
  static const RetailIconData fingerprint = RetailIconData.fingerprint;

  /// fingerprint-three
  static const RetailIconData fingerprintThree = RetailIconData.fingerprintThree;

  /// fingerprint-two
  static const RetailIconData fingerprintTwo = RetailIconData.fingerprintTwo;

  /// first
  static const RetailIconData first = RetailIconData.first;

  /// fist
  static const RetailIconData fist = RetailIconData.fist;

  /// five
  static const RetailIconData five = RetailIconData.five;

  /// five-five
  static const RetailIconData fiveFive = RetailIconData.fiveFive;

  /// flirt
  static const RetailIconData flirt = RetailIconData.flirt;

  /// four
  static const RetailIconData four = RetailIconData.four;

  /// four-four
  static const RetailIconData fourFour = RetailIconData.fourFour;

  /// good
  static const RetailIconData good = RetailIconData.good;

  /// good-one
  static const RetailIconData goodOne = RetailIconData.goodOne;

  /// good-two
  static const RetailIconData goodTwo = RetailIconData.goodTwo;

  /// hand-down
  static const RetailIconData handDown = RetailIconData.handDown;

  /// hand-drag
  static const RetailIconData handDrag = RetailIconData.handDrag;

  /// hand-left
  static const RetailIconData handLeft = RetailIconData.handLeft;

  /// hand-right
  static const RetailIconData handRight = RetailIconData.handRight;

  /// hand-up
  static const RetailIconData handUp = RetailIconData.handUp;

  /// hi
  static const RetailIconData hi = RetailIconData.hi;

  /// hold
  static const RetailIconData hold = RetailIconData.hold;

  /// hold-interface
  static const RetailIconData holdInterface = RetailIconData.holdInterface;

  /// hold-seeds
  static const RetailIconData holdSeeds = RetailIconData.holdSeeds;

  /// index-finger
  static const RetailIconData indexFinger = RetailIconData.indexFinger;

  /// mayura-gesture
  static const RetailIconData mayuraGesture = RetailIconData.mayuraGesture;

  /// middle-finger
  static const RetailIconData middleFinger = RetailIconData.middleFinger;

  /// move
  static const RetailIconData move = RetailIconData.move;

  /// okay
  static const RetailIconData okay = RetailIconData.okay;

  /// one
  static const RetailIconData one = RetailIconData.one;

  /// one-one
  static const RetailIconData oneOne = RetailIconData.oneOne;

  /// open
  static const RetailIconData open = RetailIconData.open;

  /// palm
  static const RetailIconData palm = RetailIconData.palm;

  /// point
  static const RetailIconData point = RetailIconData.point;

  /// point-out
  static const RetailIconData pointOut = RetailIconData.pointOut;

  /// press
  static const RetailIconData press = RetailIconData.press;

  /// reload
  static const RetailIconData reload = RetailIconData.reload;

  /// respect
  static const RetailIconData respect = RetailIconData.respect;

  /// rock
  static const RetailIconData rock = RetailIconData.rock;

  /// rock-gesture
  static const RetailIconData rockGesture = RetailIconData.rockGesture;

  /// six
  static const RetailIconData six = RetailIconData.six;

  /// spider-man
  static const RetailIconData spiderMan = RetailIconData.spiderMan;

  /// swing
  static const RetailIconData swing = RetailIconData.swing;

  /// three
  static const RetailIconData three = RetailIconData.three;

  /// three-three
  static const RetailIconData threeThree = RetailIconData.threeThree;

  /// thumbs-down
  static const RetailIconData thumbsDown = RetailIconData.thumbsDown;

  /// thumbs-up
  static const RetailIconData thumbsUp = RetailIconData.thumbsUp;

  /// turn-on
  static const RetailIconData turnOn = RetailIconData.turnOn;

  /// two
  static const RetailIconData two = RetailIconData.two;

  /// two-fingers
  static const RetailIconData twoFingers = RetailIconData.twoFingers;

  /// two-hands
  static const RetailIconData twoHands = RetailIconData.twoHands;

  /// two-two
  static const RetailIconData twoTwo = RetailIconData.twoTwo;

  /// yep
  static const RetailIconData yep = RetailIconData.yep;

  /// zoom
  static const RetailIconData zoom = RetailIconData.zoom;

}

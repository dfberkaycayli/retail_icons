// GENERATED FILE — DO NOT EDIT BY HAND.
// Run `cd tool && npm run generate` to regenerate.
//
// Category: abstract

import '../retail_icon_data.dart';

// ignore_for_file: lines_longer_than_80_chars, constant_identifier_names

/// IconPark icons — category: abstract
abstract final class RetailIconsAbstract {
  /// ad-product
  static const RetailIconData adProduct = RetailIconData.adProduct;

  /// api-app
  static const RetailIconData apiApp = RetailIconData.apiApp;

  /// app-switch
  static const RetailIconData appSwitch = RetailIconData.appSwitch;

  /// application-one
  static const RetailIconData applicationOne = RetailIconData.applicationOne;

  /// association
  static const RetailIconData association = RetailIconData.association;

  /// asterisk
  static const RetailIconData asterisk = RetailIconData.asterisk;

  /// badge
  static const RetailIconData badge = RetailIconData.badge;

  /// benz
  static const RetailIconData benz = RetailIconData.benz;

  /// blocks-and-arrows
  static const RetailIconData blocksAndArrows = RetailIconData.blocksAndArrows;

  /// category-management
  static const RetailIconData categoryManagement = RetailIconData.categoryManagement;

  /// circle-five-line
  static const RetailIconData circleFiveLine = RetailIconData.circleFiveLine;

  /// circle-four
  static const RetailIconData circleFour = RetailIconData.circleFour;

  /// circle-four-line
  static const RetailIconData circleFourLine = RetailIconData.circleFourLine;

  /// circle-three
  static const RetailIconData circleThree = RetailIconData.circleThree;

  /// circle-two-line
  static const RetailIconData circleTwoLine = RetailIconData.circleTwoLine;

  /// circles-and-triangles
  static const RetailIconData circlesAndTriangles = RetailIconData.circlesAndTriangles;

  /// circles-seven
  static const RetailIconData circlesSeven = RetailIconData.circlesSeven;

  /// circular-connection
  static const RetailIconData circularConnection = RetailIconData.circularConnection;

  /// cone
  static const RetailIconData cone = RetailIconData.cone;

  /// cones
  static const RetailIconData cones = RetailIconData.cones;

  /// converging-gateway
  static const RetailIconData convergingGateway = RetailIconData.convergingGateway;

  /// coordinate-system
  static const RetailIconData coordinateSystem = RetailIconData.coordinateSystem;

  /// cross-ring
  static const RetailIconData crossRing = RetailIconData.crossRing;

  /// cross-ring-two
  static const RetailIconData crossRingTwo = RetailIconData.crossRingTwo;

  /// crown-two
  static const RetailIconData crownTwo = RetailIconData.crownTwo;

  /// cube
  static const RetailIconData cube = RetailIconData.cube;

  /// cube-five
  static const RetailIconData cubeFive = RetailIconData.cubeFive;

  /// cube-four
  static const RetailIconData cubeFour = RetailIconData.cubeFour;

  /// cube-three
  static const RetailIconData cubeThree = RetailIconData.cubeThree;

  /// cube-two
  static const RetailIconData cubeTwo = RetailIconData.cubeTwo;

  /// cycle-arrow
  static const RetailIconData cycleArrow = RetailIconData.cycleArrow;

  /// cycle-one
  static const RetailIconData cycleOne = RetailIconData.cycleOne;

  /// cylinder
  static const RetailIconData cylinder = RetailIconData.cylinder;

  /// display
  static const RetailIconData display = RetailIconData.display;

  /// endless
  static const RetailIconData endless = RetailIconData.endless;

  /// error-prompt
  static const RetailIconData errorPrompt = RetailIconData.errorPrompt;

  /// exclusive-gateway
  static const RetailIconData exclusiveGateway = RetailIconData.exclusiveGateway;

  /// export
  static const RetailIconData exportIcon = RetailIconData.exportIcon;

  /// external-transmission
  static const RetailIconData externalTransmission = RetailIconData.externalTransmission;

  /// figma-component
  static const RetailIconData figmaComponent = RetailIconData.figmaComponent;

  /// figma-reset-instance
  static const RetailIconData figmaResetInstance = RetailIconData.figmaResetInstance;

  /// five-ellipses
  static const RetailIconData fiveEllipses = RetailIconData.fiveEllipses;

  /// four-arrows
  static const RetailIconData fourArrows = RetailIconData.fourArrows;

  /// four-leaves
  static const RetailIconData fourLeaves = RetailIconData.fourLeaves;

  /// four-point-connection
  static const RetailIconData fourPointConnection = RetailIconData.fourPointConnection;

  /// four-round-point-connection
  static const RetailIconData fourRoundPointConnection = RetailIconData.fourRoundPointConnection;

  /// game-emoji
  static const RetailIconData gameEmoji = RetailIconData.gameEmoji;

  /// geometric-flowers
  static const RetailIconData geometricFlowers = RetailIconData.geometricFlowers;

  /// graphic-design
  static const RetailIconData graphicDesign = RetailIconData.graphicDesign;

  /// graphic-design-two
  static const RetailIconData graphicDesignTwo = RetailIconData.graphicDesignTwo;

  /// graphic-stitching
  static const RetailIconData graphicStitching = RetailIconData.graphicStitching;

  /// graphic-stitching-four
  static const RetailIconData graphicStitchingFour = RetailIconData.graphicStitchingFour;

  /// graphic-stitching-three
  static const RetailIconData graphicStitchingThree = RetailIconData.graphicStitchingThree;

  /// halo
  static const RetailIconData halo = RetailIconData.halo;

  /// hexagon-strip
  static const RetailIconData hexagonStrip = RetailIconData.hexagonStrip;

  /// hexagonal
  static const RetailIconData hexagonal = RetailIconData.hexagonal;

  /// inclusive-gateway
  static const RetailIconData inclusiveGateway = RetailIconData.inclusiveGateway;

  /// internal-data
  static const RetailIconData internalData = RetailIconData.internalData;

  /// internal-transmission
  static const RetailIconData internalTransmission = RetailIconData.internalTransmission;

  /// keyline
  static const RetailIconData keyline = RetailIconData.keyline;

  /// misaligned-semicircle
  static const RetailIconData misalignedSemicircle = RetailIconData.misalignedSemicircle;

  /// multi-circular
  static const RetailIconData multiCircular = RetailIconData.multiCircular;

  /// multi-rectangle
  static const RetailIconData multiRectangle = RetailIconData.multiRectangle;

  /// multi-ring
  static const RetailIconData multiRing = RetailIconData.multiRing;

  /// multi-triangular
  static const RetailIconData multiTriangular = RetailIconData.multiTriangular;

  /// multi-triangular-four
  static const RetailIconData multiTriangularFour = RetailIconData.multiTriangularFour;

  /// multi-triangular-three
  static const RetailIconData multiTriangularThree = RetailIconData.multiTriangularThree;

  /// multi-triangular-two
  static const RetailIconData multiTriangularTwo = RetailIconData.multiTriangularTwo;

  /// multilayer-sphere
  static const RetailIconData multilayerSphere = RetailIconData.multilayerSphere;

  /// nested-arrows
  static const RetailIconData nestedArrows = RetailIconData.nestedArrows;

  /// nine-points-connected
  static const RetailIconData ninePointsConnected = RetailIconData.ninePointsConnected;

  /// one-third-rotation
  static const RetailIconData oneThirdRotation = RetailIconData.oneThirdRotation;

  /// open-one
  static const RetailIconData openOne = RetailIconData.openOne;

  /// outbound
  static const RetailIconData outbound = RetailIconData.outbound;

  /// oval-love
  static const RetailIconData ovalLove = RetailIconData.ovalLove;

  /// oval-love-two
  static const RetailIconData ovalLoveTwo = RetailIconData.ovalLoveTwo;

  /// parallel-gateway
  static const RetailIconData parallelGateway = RetailIconData.parallelGateway;

  /// process-line
  static const RetailIconData processLine = RetailIconData.processLine;

  /// quadrangular-pyramid
  static const RetailIconData quadrangularPyramid = RetailIconData.quadrangularPyramid;

  /// rectangle-tear
  static const RetailIconData rectangleTear = RetailIconData.rectangleTear;

  /// rectangle-x
  static const RetailIconData rectangleX = RetailIconData.rectangleX;

  /// rectangular-circular-connection
  static const RetailIconData rectangularCircularConnection = RetailIconData.rectangularCircularConnection;

  /// rectangular-circular-separation
  static const RetailIconData rectangularCircularSeparation = RetailIconData.rectangularCircularSeparation;

  /// rectangular-vertebra
  static const RetailIconData rectangularVertebra = RetailIconData.rectangularVertebra;

  /// refraction
  static const RetailIconData refraction = RetailIconData.refraction;

  /// reverse-operation-in
  static const RetailIconData reverseOperationIn = RetailIconData.reverseOperationIn;

  /// reverse-operation-out
  static const RetailIconData reverseOperationOut = RetailIconData.reverseOperationOut;

  /// right-run
  static const RetailIconData rightRun = RetailIconData.rightRun;

  /// round-caliper
  static const RetailIconData roundCaliper = RetailIconData.roundCaliper;

  /// round-distortion
  static const RetailIconData roundDistortion = RetailIconData.roundDistortion;

  /// round-mask
  static const RetailIconData roundMask = RetailIconData.roundMask;

  /// run-left
  static const RetailIconData runLeft = RetailIconData.runLeft;

  /// sales-report
  static const RetailIconData salesReport = RetailIconData.salesReport;

  /// seo
  static const RetailIconData seo = RetailIconData.seo;

  /// six-circular-connection
  static const RetailIconData sixCircularConnection = RetailIconData.sixCircularConnection;

  /// six-points
  static const RetailIconData sixPoints = RetailIconData.sixPoints;

  /// smart-optimization
  static const RetailIconData smartOptimization = RetailIconData.smartOptimization;

  /// sphere
  static const RetailIconData sphere = RetailIconData.sphere;

  /// split-branch
  static const RetailIconData splitBranch = RetailIconData.splitBranch;

  /// stereo-nesting
  static const RetailIconData stereoNesting = RetailIconData.stereoNesting;

  /// stereo-perspective
  static const RetailIconData stereoPerspective = RetailIconData.stereoPerspective;

  /// switch-contrast
  static const RetailIconData switchContrast = RetailIconData.switchContrast;

  /// symbol-double-x
  static const RetailIconData symbolDoubleX = RetailIconData.symbolDoubleX;

  /// symmetry
  static const RetailIconData symmetry = RetailIconData.symmetry;

  /// sync
  static const RetailIconData syncIcon = RetailIconData.syncIcon;

  /// table-report
  static const RetailIconData tableReport = RetailIconData.tableReport;

  /// texture
  static const RetailIconData texture = RetailIconData.texture;

  /// texture-two
  static const RetailIconData textureTwo = RetailIconData.textureTwo;

  /// three-hexagons
  static const RetailIconData threeHexagons = RetailIconData.threeHexagons;

  /// three-slashes
  static const RetailIconData threeSlashes = RetailIconData.threeSlashes;

  /// three-triangles
  static const RetailIconData threeTriangles = RetailIconData.threeTriangles;

  /// treasure-chest
  static const RetailIconData treasureChest = RetailIconData.treasureChest;

  /// triangle-round-rectangle
  static const RetailIconData triangleRoundRectangle = RetailIconData.triangleRoundRectangle;

  /// triangular-pyramid
  static const RetailIconData triangularPyramid = RetailIconData.triangularPyramid;

  /// two-ellipses
  static const RetailIconData twoEllipses = RetailIconData.twoEllipses;

  /// two-semicircles
  static const RetailIconData twoSemicircles = RetailIconData.twoSemicircles;

  /// two-triangles
  static const RetailIconData twoTriangles = RetailIconData.twoTriangles;

  /// two-triangles-two
  static const RetailIconData twoTrianglesTwo = RetailIconData.twoTrianglesTwo;

  /// update-rotation
  static const RetailIconData updateRotation = RetailIconData.updateRotation;

  /// warehousing
  static const RetailIconData warehousing = RetailIconData.warehousing;

  /// whirlwind
  static const RetailIconData whirlwind = RetailIconData.whirlwind;

}

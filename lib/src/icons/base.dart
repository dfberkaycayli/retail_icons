// GENERATED FILE — DO NOT EDIT BY HAND.
// Run `cd tool && npm run generate` to regenerate.
//
// Category: base

import '../retail_icon_data.dart';

// ignore_for_file: lines_longer_than_80_chars, constant_identifier_names

/// IconPark icons — category: base
abstract final class RetailIconsBase {
  /// aiming
  static const RetailIconData aiming = RetailIconData.aiming;

  /// all-application
  static const RetailIconData allApplication = RetailIconData.allApplication;

  /// bill
  static const RetailIconData bill = RetailIconData.bill;

  /// bookmark
  static const RetailIconData bookmark = RetailIconData.bookmark;

  /// bookmark-one
  static const RetailIconData bookmarkOne = RetailIconData.bookmarkOne;

  /// camera
  static const RetailIconData camera = RetailIconData.camera;

  /// config
  static const RetailIconData config = RetailIconData.config;

  /// delete-two
  static const RetailIconData deleteTwo = RetailIconData.deleteTwo;

  /// dislike
  static const RetailIconData dislike = RetailIconData.dislike;

  /// dislike-two
  static const RetailIconData dislikeTwo = RetailIconData.dislikeTwo;

  /// equalizer
  static const RetailIconData equalizer = RetailIconData.equalizer;

  /// female
  static const RetailIconData female = RetailIconData.female;

  /// hamburger-button
  static const RetailIconData hamburgerButton = RetailIconData.hamburgerButton;

  /// home
  static const RetailIconData home = RetailIconData.home;

  /// hourglass-full
  static const RetailIconData hourglassFull = RetailIconData.hourglassFull;

  /// hourglass-null
  static const RetailIconData hourglassNull = RetailIconData.hourglassNull;

  /// lightning
  static const RetailIconData lightning = RetailIconData.lightning;

  /// like
  static const RetailIconData like = RetailIconData.like;

  /// loading
  static const RetailIconData loading = RetailIconData.loading;

  /// loading-four
  static const RetailIconData loadingFour = RetailIconData.loadingFour;

  /// male
  static const RetailIconData male = RetailIconData.male;

  /// more
  static const RetailIconData more = RetailIconData.more;

  /// more-app
  static const RetailIconData moreApp = RetailIconData.moreApp;

  /// more-one
  static const RetailIconData moreOne = RetailIconData.moreOne;

  /// more-two
  static const RetailIconData moreTwo = RetailIconData.moreTwo;

  /// pic
  static const RetailIconData pic = RetailIconData.pic;

  /// power
  static const RetailIconData power = RetailIconData.power;

  /// preview-close
  static const RetailIconData previewClose = RetailIconData.previewClose;

  /// preview-close-one
  static const RetailIconData previewCloseOne = RetailIconData.previewCloseOne;

  /// preview-open
  static const RetailIconData previewOpen = RetailIconData.previewOpen;

  /// radar
  static const RetailIconData radar = RetailIconData.radar;

  /// refresh
  static const RetailIconData refresh = RetailIconData.refresh;

  /// rss
  static const RetailIconData rss = RetailIconData.rss;

  /// save
  static const RetailIconData save = RetailIconData.save;

  /// save-one
  static const RetailIconData saveOne = RetailIconData.saveOne;

  /// search
  static const RetailIconData search = RetailIconData.search;

  /// setting
  static const RetailIconData setting = RetailIconData.setting;

  /// setting-config
  static const RetailIconData settingConfig = RetailIconData.settingConfig;

  /// setting-one
  static const RetailIconData settingOne = RetailIconData.settingOne;

  /// setting-three
  static const RetailIconData settingThree = RetailIconData.settingThree;

  /// setting-two
  static const RetailIconData settingTwo = RetailIconData.settingTwo;

  /// share
  static const RetailIconData share = RetailIconData.share;

  /// share-three
  static const RetailIconData shareThree = RetailIconData.shareThree;

  /// sleep
  static const RetailIconData sleep = RetailIconData.sleep;

  /// system
  static const RetailIconData system = RetailIconData.system;

  /// tag
  static const RetailIconData tag = RetailIconData.tag;

  /// tag-one
  static const RetailIconData tagOne = RetailIconData.tagOne;

  /// tips
  static const RetailIconData tips = RetailIconData.tips;

  /// tool
  static const RetailIconData tool = RetailIconData.tool;

  /// translate
  static const RetailIconData translate = RetailIconData.translate;

  /// unlike
  static const RetailIconData unlike = RetailIconData.unlike;

  /// waterfalls-h
  static const RetailIconData waterfallsH = RetailIconData.waterfallsH;

  /// waterfalls-v
  static const RetailIconData waterfallsV = RetailIconData.waterfallsV;

  /// zoom-in
  static const RetailIconData zoomIn = RetailIconData.zoomIn;

  /// zoom-out
  static const RetailIconData zoomOut = RetailIconData.zoomOut;

}
